# 更新日志 / Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 与 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [1.0.4] - 2026-09-16

### 修复
- **深色模式全面重做**：清除全项目硬编码的白色卡片 / 浅色背景 / 黑色分割线，背景、卡片、导航栏、弹窗、输入框、提示条全部跟随主题与深色模式反色。
- **修复主界面使用一段时间后灰屏 / 花屏**：Android 端回退到成熟稳定的 Skia 渲染（关闭部分设备不稳定的 Impeller）；新增全局构建错误兜底，单个组件异常不再导致整屏灰死；收紧图片内存缓存，降低长时间使用后的内存压力。
- **修复分类自定义图片图标选择时闪退**：宿主 Activity 改为 `FlutterFragmentActivity`（image_picker / image_cropper 的硬性要求）；选图限制读入尺寸避免大图 OOM；相册或裁剪失败时给出提示并退回原图，不再卡退。

### 新增
- **添加应用支持搜索与字母索引**：分类页「添加应用」弹窗顶部新增搜索框（按名称 / 包名过滤），右侧新增 A-Z 字母索引条（中文按拼音首字母），点击字母快速定位到对应分组。
- **主题升级为整套界面色调**：不再只改强调色，背景、卡片、导航栏、输入框、弹窗都会随主题色调整体变化；预设色调由 8 个扩充到 12 个。

### 改进
- 主界面趋势图由柱状图改为**平滑波形图**（渐变面积折线图），应用详情页同步更换。
- 对比页图表、未分类提示条、WebDAV 结果卡片、备份提示卡片等在深色模式下的配色修正。

## [1.0.3] - 2026-09-16

### 新增
- **分类图标支持自定义图片**：编辑分类时可从相册选择图片，内置 1:1 方形裁剪，可随时清除恢复图标库图标。
- **主题切换**：设置 → 外观，支持「跟随系统 / 浅色 / 深色」三种模式，以及 8 种强调色（青绿、蓝、紫、粉、橙、绿、棕、蓝灰）。
- **多语言**：中文 / English，可跟随系统或手动切换。
- **自定义日期区间统计**：主界面新增「自定义区间」，可任选起止日期（最近 5 年内），显示该区间总时长、应用排行、分类占比与每日柱状图。
- **关于页**：版本信息、GitHub 开源仓库、检查更新、MIT 开源许可证、赞赏入口。
- 项目开源：MIT 许可证、README、CHANGELOG、.gitignore 等标准开源物料。

### 改进
- **环形图交互**：点击分类扇区可放大高亮，中心显示该分类名称与使用时长；图例同步高亮，再次点击取消。
- **24 小时分布图重做**：去掉满屏灰色背景条，只显示有使用的小时，柱体渐变圆角，空数据居中提示。
- **设置页精简**：「立即采集一次」移除；「导入系统历史记录」导入一次后自动隐藏。
- **性能优化**：
  - 周期后台采集不再触发 UI 重建（静默采集），避免每 30 秒整页刷新导致的卡顿；
  - 应用图标按显示尺寸缩小解码（cacheWidth），降低内存与解码开销；
  - 柱状图 / 环形图交互渲染更轻量。
- 暗色主题下全面适配文字与卡片颜色。

### 修复
- 多语言切换后全树正确重建。
- Windows / Android 双端主题、日期选择器等组件本地化。

## [1.0.2] - 2026-09-15

### 改进
- 性能优化：原生图标缓存命中直接复用，避免周期性全量重生成；应用清单与应用事件低频刷新；初始化后台延迟刷新不阻塞首屏；统计页分类一次查询。

## [1.0.1] - 2026-09-15

### 修复
- 修复启动黑屏：Provider 缺少 child 导致组件树整体构建失败；同时修复设置页水波纹调试断言。

## [1.0.0] - 2026-09-15

首个正式签名版本。

### 功能
- 自带游戏 / 小说 / 动漫 / 影视分类，可增删改、可自定义新分类（多标签）。
- 日 / 周 / 月 / 年 / 永久统计，卸载保留数据、重装叠加。
- 导入系统历史使用时间。
- 应用详情：图表、平均 / 连续 / 峰值、可编辑安装日期、评分、描述、来源链接（GitHub / 网盘 / Telegram 自动跳转）、单日限额。
- WebDAV 手动增量同步（只增不覆盖）、手动完整备份与覆盖恢复。
- 对比视图（今日 vs 昨日、本周 vs 上周等）。
- 首次启动权限引导（使用情况、电池白名单、悬浮窗、无障碍、通知、自启动）。
